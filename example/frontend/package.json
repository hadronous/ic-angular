{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "dfx generate example_backend && ng build example",
    "start": "dfx generate example_backend && ng serve example"
  },
  "devDependencies": {
    "@hadronous/ic-angular": "workspace:*"
  }
}
